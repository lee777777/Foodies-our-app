import React from 'react';
import {View, Text,  StyleSheet, TouchableOpacity} from 'react-native';

const Recipes = () =>{
    return(
        <View ><Text>Add  recipes here</Text></View>
    );
};

const styles = StyleSheet.create({

});
export default Recipes;